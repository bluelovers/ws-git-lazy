{
  "name": "ws-git-lazy",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lerna-publish": "lerna publish",
    "run-workspace-publish": "yarn workspace-publish",
    "run-workspace-version": "yarn workspace-version",
    "sort-package-json": "oao run-script \"sort-package-json2\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "lerna": "^3.13.1",
    "workspace-release": "^0.4.5"
  },
  "devDependencies": {
    "@bluelovers/tsconfig": "^1.0.1"
  },
  "resolutions": {}
}
